-- ======================================
-- 15-06-2026
-- ======================================
ALTER TABLE orders
ADD COLUMN city_id BIGINT UNSIGNED NULL AFTER courier_id,
ADD COLUMN zone_id BIGINT UNSIGNED NULL AFTER city_id;

-- ======================================
-- 14-06-2026
-- ======================================
ALTER TABLE orders
ADD COLUMN remarks TEXT NULL AFTER notes,
ADD COLUMN courier_id BIGINT UNSIGNED NULL AFTER remarks,
ADD COLUMN consignment_id VARCHAR(255) NULL AFTER courier_id,
ADD COLUMN tracking_code VARCHAR(255) NULL AFTER consignment_id,
ADD COLUMN tracking_url VARCHAR(255) NULL AFTER tracking_code;

ALTER TABLE orders
ADD CONSTRAINT fk_orders_courier
FOREIGN KEY (courier_id) REFERENCES couriers(id)
ON DELETE SET NULL;
